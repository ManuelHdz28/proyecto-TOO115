<Window x:Class="TOO_SanBenito.UI.login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Login - San Benito"
        Height="500"
        Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#10B981"/>
    </Window.Resources>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <StackPanel Grid.Row="0" Margin="0,0,0,40">
            <TextBlock Text="Farmacia San Benito"
                       FontSize="28"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Sistema de Gestión"
                       Foreground="#64748B"
                       FontSize="14"
                       Margin="0,8,0,0"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Selección de Rol -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <TextBlock Text="Selecciona tu tipo de acceso"
                       FontSize="16"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,30"
                       Foreground="#64748B"/>

            <!-- Botón Administrador -->
            <Border BorderBrush="#E2E8F0"
                    BorderThickness="2"
                    CornerRadius="8"
                    Margin="0,0,0,15"
                    Background="White">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="Background" Value="#F0F7FF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Button x:Name="BtnLoginAdmin"
                        Click="BtnLoginAdmin_Click"
                        Height="80"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand">
                    <StackPanel>
                        <TextBlock Text="👨‍💼 Administrador"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryBrush}"/>
                        <TextBlock Text="Reportes, productos y configuración"
                                   FontSize="12"
                                   Foreground="#64748B"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Button>
            </Border>

            <!-- Botón Vendedor -->
            <Border BorderBrush="#E2E8F0"
                    BorderThickness="2"
                    CornerRadius="8"
                    Background="White">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource SuccessBrush}"/>
                                <Setter Property="Background" Value="#F0FDF4"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Button x:Name="BtnLoginVendedor"
                        Click="BtnLoginVendedor_Click"
                        Height="80"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand">
                    <StackPanel>
                        <TextBlock Text="👤 Vendedor"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource SuccessBrush}"/>
                        <TextBlock Text="Punto de venta y consultas del día"
                                   FontSize="12"
                                   Foreground="#64748B"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Button>
            </Border>
        </StackPanel>

        <!-- Loading overlay (mantener compatibilidad) -->
        <Border x:Name="LoadingOverlay"
                Grid.RowSpan="2"
                Background="#AA000000"
                Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="6"/>
                <TextBlock Text="Iniciando sesión..."
                           Margin="0,16,0,0"
                           FontSize="14"
                           Foreground="White"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
