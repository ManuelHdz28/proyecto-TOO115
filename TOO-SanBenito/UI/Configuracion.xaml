<Page x:Class="TOO_SanBenito.UI.Configuracion"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Configuración">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2563EB"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#10B981"/>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#F8FAFC">
        <StackPanel Margin="40" MaxWidth="800">
            <TextBlock Text="⚙ Configuración"
                       FontSize="28"
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>
            <TextBlock Text="Configuración general del sistema"
                       FontSize="14"
                       Foreground="#64748B"
                       Margin="0,0,0,40"/>

            <!-- Información de la Farmacia -->
            <Border Background="White" CornerRadius="8" Padding="30" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Información de la Farmacia"
                               FontSize="18"
                               FontWeight="Bold"
                               Margin="0,0,0,20"/>

                    <TextBlock Text="Nombre de la Farmacia" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox x:Name="TxtNombreFarmacia"
                             Text="Farmacia San Benito"
                             Padding="12"
                             Margin="0,0,0,15"
                             FontSize="14"/>

                    <TextBlock Text="Dirección" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox x:Name="TxtDireccion"
                             Text="Av. Principal #123, Centro, Ciudad"
                             Padding="12"
                             Margin="0,0,0,15"
                             FontSize="14"/>

                    <TextBlock Text="Teléfono" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox x:Name="TxtTelefono"
                             Text="(503) 2222-3333"
                             Padding="12"
                             Margin="0,0,0,15"
                             FontSize="14"/>

                    <TextBlock Text="Email" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox x:Name="TxtEmail"
                             Text="info@farmaciasanbenito.com"
                             Padding="12"
                             Margin="0,0,0,20"
                             FontSize="14"/>

                    <Button Content="💾 Guardar Cambios"
                            Background="{StaticResource PrimaryBrush}"
                            Foreground="White"
                            Padding="20,12"
                            BorderThickness="0"
                            Cursor="Hand"
                            FontWeight="SemiBold"
                            HorizontalAlignment="Right"
                            Click="BtnGuardarInfo_Click"/>
                </StackPanel>
            </Border>

            <!-- Configuración del Sistema -->
            <Border Background="White" CornerRadius="8" Padding="30" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Configuración del Sistema"
                               FontSize="18"
                               FontWeight="Bold"
                               Margin="0,0,0,20"/>

                    <Grid Margin="0,0,0,15">
                        <TextBlock Text="Stock Mínimo de Alerta" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBox x:Name="TxtStockMinimo"
                                     Text="10"
                                     Width="80"
                                     Padding="8"
                                     TextAlignment="Center"/>
                            <TextBlock Text=" unidades"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0"
                                       Foreground="#64748B"/>
                        </StackPanel>
                    </Grid>

                    <Separator Margin="0,10" Background="#E2E8F0"/>

                    <Grid Margin="0,15,0,15">
                        <TextBlock Text="Horario de Atención" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBox x:Name="TxtHorarioInicio"
                                     Text="08:00"
                                     Width="70"
                                     Padding="8"
                                     TextAlignment="Center"/>
                            <TextBlock Text=" - "
                                       VerticalAlignment="Center"
                                       Margin="8,0"/>
                            <TextBox x:Name="TxtHorarioFin"
                                     Text="20:00"
                                     Width="70"
                                     Padding="8"
                                     TextAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <Separator Margin="0,10" Background="#E2E8F0"/>

                    <Grid Margin="0,15,0,20">
                        <TextBlock Text="Notificaciones de Stock Bajo" VerticalAlignment="Center" FontWeight="SemiBold"/>
                        <CheckBox x:Name="ChkNotificaciones"
                                  IsChecked="True"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Center"/>
                    </Grid>

                    <Button Content="💾 Guardar Configuración"
                            Background="{StaticResource PrimaryBrush}"
                            Foreground="White"
                            Padding="20,12"
                            BorderThickness="0"
                            Cursor="Hand"
                            FontWeight="SemiBold"
                            HorizontalAlignment="Right"
                            Click="BtnGuardarConfig_Click"/>
                </StackPanel>
            </Border>

            <!-- Información del Sistema -->
            <Border Background="White" CornerRadius="8" Padding="30">
                <StackPanel>
                    <TextBlock Text="Información del Sistema"
                               FontSize="18"
                               FontWeight="Bold"
                               Margin="0,0,0,20"/>

                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Versión del Sistema:" Foreground="#64748B"/>
                        <TextBlock Text="1.0.0 MVP" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                    </Grid>

                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Última Actualización:" Foreground="#64748B"/>
                        <TextBlock Text="04 de Noviembre, 2025" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                    </Grid>

                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="Base de Datos:" Foreground="#64748B"/>
                        <TextBlock Text="MySQL (Desarrollo)" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                    </Grid>

                    <Grid>
                        <TextBlock Text="Desarrollado por:" Foreground="#64748B"/>
                        <TextBlock Text="Equipo TOO" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
